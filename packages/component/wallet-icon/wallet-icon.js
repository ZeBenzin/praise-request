import React, { PureComponent } from "react";
import styles from "./wallet-icon.scss";

class SearchIcon extends PureComponent {
  render() {
    return <span className={styles.walletIcon} />;
  }
}

export default SearchIcon;
