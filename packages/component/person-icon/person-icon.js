import React, { PureComponent } from "react";
import styles from "./person-icon.scss";

class PersonIcon extends PureComponent {
  render() {
    return <span className={styles.personIcon} />;
  }
}

export default PersonIcon;
